---
phase: 01-build-hygiene
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components/team/team-invite-form.tsx
  - components/auth/invite-accept-form.tsx
  - components/enhanced-invite-dialog.tsx
  - components/bulk-invite-dialog.tsx
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npx tsc --noEmit reports zero errors for all 4 component files"
    - "npx eslint on all 4 component files reports zero violations"
    - "No @ts-nocheck directive remains in any component file"
    - "No catch (err: any) blocks remain in any component file"
  artifacts:
    - "components/team/team-invite-form.tsx has no @ts-nocheck"
    - "components/auth/invite-accept-form.tsx has no @ts-nocheck"
    - "components/enhanced-invite-dialog.tsx has no @ts-nocheck"
    - "components/bulk-invite-dialog.tsx has no @ts-nocheck"
  key_links:
    - "All 4 components import getErrorMessage from @/lib/errors (where they access error messages in catch blocks)"
    - "All 4 components consume typed return types from lib/team-rpc.ts (Plan 01 output)"
---

<objective>
Remove @ts-nocheck from the 4 team component files and narrow all catch blocks.

These files depend on lib/team-rpc.ts (typed in Plan 01). With the shared dependency typed and the getErrorMessage helper available, each component can be fixed independently. The catch blocks that use err.message (4 instances across 2 files) and catch (err: any) must be narrowed using the pattern proven in Plan 01.

Purpose: HYG-03 requires zero @ts-nocheck directives. HYG-02 requires zero no-explicit-any violations. The 4 components are the remaining @ts-nocheck files after Plan 01 clears team-rpc.ts.

Output: 4 component files fully typed, zero @ts-nocheck, zero catch (err: any).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-hygiene/01-RESEARCH.md
</context>

<tasks>

<!-- planner-discipline-allow: @ts-nocheck -->
<!-- planner-discipline-allow: catch (err: any) -->
<task type="auto">
  <name>Task 1: Remove @ts-nocheck from invite-accept-form.tsx</name>
  <files>components/auth/invite-accept-form.tsx</files>
  <read_first>
    - components/auth/invite-accept-form.tsx (FULL — 321 lines; @ts-nocheck is line 1; has 1 catch (err: any) at line 118 with err.message at line 119)
    - lib/errors.ts (getErrorMessage signature — created in Plan 01 Task 1)
    - lib/team-rpc.ts (the acceptInvite method signature and AcceptInviteResult interface — typed in Plan 01)
    - lib/token-security.ts (the validateInvite and acceptInvite methods consumed by this component)
  </read_first>
  <action>
Remove the // @ts-nocheck directive from line 1 of components/auth/invite-accept-form.tsx. Fix all surfaced TS errors:

1. Narrow the catch block at line ~118: change catch (err: any) { setError(err.message || 'Failed to accept invite'); } to catch (err) { setError(getErrorMessage(err) || 'Failed to accept invite'); }. Import getErrorMessage from @/lib/errors.

2. Fix any remaining type errors: implicit any parameters in event handlers, untyped state variables, props without interfaces. The component consumes acceptInvite and validateInvite from the team/token modules — their return types are now typed from Plan 01, so the consumer variables should infer correctly. If not, add explicit type annotations matching the imported interfaces (AcceptInviteResult, etc.).

3. Ensure all useState declarations have explicit generic types where inference is ambiguous. Ensure all event handler parameters (React.FormEvent, React.ChangeEvent, etc.) are typed.

Do NOT add eslint-disable comments to suppress errors. Do NOT change component logic or UI. Fix the types, not the behavior.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "@ts-nocheck" components/auth/invite-accept-form.tsx | grep -q "^0$" && echo "NOCHECK_REMOVED" || echo "NOCHECK_PRESENT"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "catch (err: any)\|catch (error: any)" components/auth/invite-accept-form.tsx | grep -q "^0$" && echo "NO_ANY_CATCH" || echo "ANY_CATCH_FOUND"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint components/auth/invite-accept-form.tsx 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
  </verify>
  <done>
    - Zero @ts-nocheck directives in invite-accept-form.tsx
    - Zero catch (err: any) blocks
    - npx eslint passes clean on the file
    - npx tsc --noEmit reports zero errors for the file
  </done>
  <acceptance_criteria>
    - Source assertion: grep -c "@ts-nocheck" components/auth/invite-accept-form.tsx equals 0
    - Source assertion: grep -c "catch (err: any)" components/auth/invite-accept-form.tsx equals 0
    - Source assertion: grep -c "getErrorMessage" components/auth/invite-accept-form.tsx is at least 1
    - Lint assertion: npx eslint components/auth/invite-accept-form.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove @ts-nocheck from team-invite-form.tsx</name>
  <files>components/team/team-invite-form.tsx</files>
  <read_first>
    - components/team/team-invite-form.tsx (FULL — 554 lines; @ts-nocheck is line 8; has 3 catch (err: any) blocks at lines 126, 148, 212 with err.message accesses at lines 127, 149, 213)
    - lib/errors.ts (getErrorMessage — created in Plan 01)
    - lib/team-rpc.ts (createInvite, checkRateLimit, bulkInvite method signatures and their return types — typed in Plan 01)
    - types/team.ts (TeamRole, CreateInviteInput, RateLimitInfo interfaces)
  </read_first>
  <action>
Remove the // @ts-nocheck directive from line 8 of components/team/team-invite-form.tsx. Fix all surfaced TS and ESLint errors:

1. Narrow all 3 catch (err: any) blocks. At line ~126: catch (err: any) { setError(err.message || "Failed to create invite"); } becomes catch (err) { setError(getErrorMessage(err) || "Failed to create invite"); }. At line ~148: catch (err: any) { setError(`Failed to check rate limit: ${err.message}`); } becomes catch (err) { setError(`Failed to check rate limit: ${getErrorMessage(err)}`); }. At line ~212: catch (err: any) { setError(err.message || "Failed to create bulk invites"); } becomes catch (err) { setError(getErrorMessage(err) || "Failed to create bulk invites"); }. Import getErrorMessage from @/lib/errors.

2. Fix remaining type errors: implicit any parameters in event handlers and map callbacks, untyped form refs, state variables without explicit types. The component consumes TeamRPC methods (createInvite, checkRateLimit, bulkInvite) and TeamService methods — all now return typed results from Plan 01. Wire the consumer variables to these types.

3. Ensure all props are typed via interfaces. Ensure all useState/useRef declarations have explicit generic types.

Do NOT add eslint-disable comments. Do NOT change component logic or UI. This is the largest of the 4 files (554 lines) — fix types methodically, working through tsc errors one at a time.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "@ts-nocheck" components/team/team-invite-form.tsx | grep -q "^0$" && echo "NOCHECK_REMOVED" || echo "NOCHECK_PRESENT"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "catch (err: any)\|catch (error: any)" components/team/team-invite-form.tsx | grep -q "^0$" && echo "NO_ANY_CATCH" || echo "ANY_CATCH_FOUND"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint components/team/team-invite-form.tsx 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
  </verify>
  <done>
    - Zero @ts-nocheck directives in team-invite-form.tsx
    - Zero catch (err: any) blocks (all 3 narrowed)
    - npx eslint passes clean on the file
    - npx tsc --noEmit reports zero errors for the file
  </done>
  <acceptance_criteria>
    - Source assertion: grep -c "@ts-nocheck" components/team/team-invite-form.tsx equals 0
    - Source assertion: grep -c "catch (err: any)" components/team/team-invite-form.tsx equals 0
    - Source assertion: grep -c "getErrorMessage" components/team/team-invite-form.tsx is at least 3
    - Lint assertion: npx eslint components/team/team-invite-form.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove @ts-nocheck from enhanced-invite-dialog.tsx and bulk-invite-dialog.tsx</name>
  <files>components/enhanced-invite-dialog.tsx, components/bulk-invite-dialog.tsx</files>
  <read_first>
    - components/enhanced-invite-dialog.tsx (FULL — 378 lines; @ts-nocheck is line 1; catch block at line ~84 uses err instanceof Error ? err.message pattern — already partially narrowed, confirm it does not use catch (err: any))
    - components/bulk-invite-dialog.tsx (FULL — 365 lines; @ts-nocheck is line 1; catch block at line ~103 uses error instanceof Error ? error.message pattern — already partially narrowed, confirm it does not use catch (err: any))
    - lib/team-rpc.ts (the invite and bulk-invite method signatures — typed in Plan 01)
    - types/team.ts (TeamRole interface for role typing)
  </read_first>
  <action>
Remove the // @ts-nocheck directive from line 1 of both files. Fix all surfaced TS errors in each:

For enhanced-invite-dialog.tsx:
1. Remove @ts-nocheck from line 1.
2. The catch block at line ~84 already uses the err instanceof Error ? err.message : "..." pattern — verify this compiles under strict mode. If it does, no change needed to the catch logic. If it surfaces a type error, import getErrorMessage from @/lib/errors and use it instead.
3. Fix remaining type errors: untyped props, implicit any parameters, state variables without types. Consume the typed return types from TeamRPC methods.

For bulk-invite-dialog.tsx:
1. Remove @ts-nocheck from line 1.
2. The catch block at line ~103 already uses the error instanceof Error ? error.message : "..." pattern — same treatment as above.
3. Fix remaining type errors: untyped props, implicit any parameters, state variables without types. The component iterates over email arrays and calls bulkInvite — wire types from the TeamRPC return types.

For both files: ensure all props are typed via interfaces, all event handlers have typed parameters, all useState declarations have explicit generics. Do NOT add eslint-disable comments. Do NOT change component logic or UI.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "@ts-nocheck" components/enhanced-invite-dialog.tsx components/bulk-invite-dialog.tsx | grep -q "^0$" && echo "NOCHECK_REMOVED" || echo "NOCHECK_PRESENT"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint components/enhanced-invite-dialog.tsx components/bulk-invite-dialog.tsx 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx tsc --noEmit 2>&1 | grep -c "enhanced-invite-dialog\|bulk-invite-dialog" | grep -q "^0$" && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
  </verify>
  <done>
    - Zero @ts-nocheck directives in enhanced-invite-dialog.tsx and bulk-invite-dialog.tsx
    - npx eslint passes clean on both files
    - npx tsc --noEmit reports zero errors for both files
  </done>
  <acceptance_criteria>
    - Source assertion: grep -rl "@ts-nocheck" components/ returns empty (all 4 component files from this plan + Plan 02 cleared)
    - Lint assertion: npx eslint components/enhanced-invite-dialog.tsx components/bulk-invite-dialog.tsx exits 0
    - Build assertion: npx tsc --noEmit does not report errors in either file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → RPC boundary | Components consume typed return values from lib/team-rpc.ts — the type contracts established in Plan 01 must be honored |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | team-invite-form.tsx invite creation | low | accept | Invite role parameter is typed via TeamRole ("owner"\|"editor"\|"viewer") — existing validation in TeamRPC.createInvite enforces authorization server-side |
| T-02-02 | Tampering | bulk-invite-dialog.tsx email array | low | accept | Email array is validated by the RPC function; typing the array as string[] does not change runtime validation |
</threat_model>

<verification>
After all 3 tasks:
- grep -rl "@ts-nocheck" components/ returns empty (all 4 files cleared)
- grep -rl "catch (err: any)\|catch (error: any)" components/ returns empty
- npx eslint components/team/team-invite-form.tsx components/auth/invite-accept-form.tsx components/enhanced-invite-dialog.tsx components/bulk-invite-dialog.tsx passes clean
- npx tsc --noEmit reports zero errors in any of the 4 component files
</verification>

<success_criteria>
- All 4 component files have zero @ts-nocheck directives
- All catch blocks narrowed (no catch (err: any))
- npx eslint passes on all 4 files
- npx tsc --noEmit passes on all 4 files
- The only remaining @ts-nocheck in the codebase is zero (all 5 original files cleared across Plans 01+02)
</success_criteria>

<output>
Create `.planning/phases/01-build-hygiene/01-02-SUMMARY.md` when done
</output>
