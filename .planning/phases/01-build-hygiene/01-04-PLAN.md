---
phase: 01-build-hygiene
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - app/(dashboard)/dashboard/layout.tsx
  - app/(dashboard)/dashboard/page.tsx
  - app/(dashboard)/dashboard/teams/page.tsx
  - app/(dashboard)/dashboard/teams/[slug]/page.tsx
  - next.config.ts
autonomous: false
requirements: [HYG-01, HYG-02, HYG-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm run build exits 0 with zero TypeScript errors and zero ESLint violations"
    - "next.config.ts has no ignoreBuildErrors or ignoreDuringBuilds keys"
    - "grep -rn 'eslint-disable.*no-explicit-any' app/(dashboard) returns zero results"
    - "grep -rl '@ts-nocheck' app/ components/ lib/ hooks/ utils/ types/ returns zero results"
    - "grep -rl 'trycloudflare.com' lib/ returns zero results"
  artifacts:
    - "next.config.ts typescript and eslint sections absent (default-false enforcement)"
    - "All 4 dashboard files compile without any[] casts or eslint-disable comments"
  key_links:
    - "Dashboard Supabase queries consume the typed Database generic from Plan 03 — no as any[] casts remain"
    - "npm run build is the authoritative phase gate — it runs tsc + eslint + bundle in one pass"
---

<objective>
Remove the 7 eslint-disable no-explicit-any suppressions from dashboard files by using the typed Database generic (from Plan 03), then flip the build config flags as the FINAL step (HYG-01, HYG-02).

This is the closing gate of the phase. All prior debt — @ts-nocheck (Plans 01+02), tunnel URL (Plan 03), untyped Supabase clients (Plan 03) — must be cleared before the flags come off. The dashboard files are the last consumers of the untyped query pattern: Plan 03 threaded the Database generic, which now makes their as any[] casts into type errors. This plan resolves those errors using the typed query results, then removes the ignoreBuildErrors and ignoreDuringBuilds flags from next.config.ts.

Purpose: HYG-04 requires dashboard queries use typed Supabase responses — no any casts with eslint-disable. HYG-01+HYG-02 require the build to enforce type checking and linting. The flags are the LAST commit because re-enabling them before clearing debt would fail the build with hundreds of errors (research Anti-Pattern 1).

Output: 4 dashboard files typed, next.config.ts flags removed, npm run build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-hygiene/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type dashboard layout.tsx and dashboard/page.tsx queries</name>
  <files>app/(dashboard)/dashboard/layout.tsx, app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - app/(dashboard)/dashboard/layout.tsx (FULL — 55 lines; eslint-disable at line 41, as any[] cast at line 42; query at lines 36-39 selects "teams(slug, name)" from team_members)
    - app/(dashboard)/dashboard/page.tsx (FULL — lines 1-65 critical; eslint-disable at lines 44 and 62, as any[] casts at lines 45 and 63; query at lines 39-42 selects "team_id, teams(slug, name)" and lines 50-56 selects nested shares join)
    - types/database.generated.ts (the generated Database type from Plan 03 — check the inferred shape of team_members.teams join and team_shares.shares join)
    - utils/supabase/server.ts (the createClient function — now returns createServerClient<Database> from Plan 03)
    - types/share.ts (the Share interface used in dashboard/page.tsx line 8)
  </read_first>
  <action>
Remove all eslint-disable no-explicit-any comments and as any[] casts from both files. The Database generic threaded in Plan 03 now types the Supabase query results — the casts are no longer needed.

For dashboard/layout.tsx:
1. Remove the eslint-disable comment at line ~41 and the as any[] cast at line ~42. The memberships query (.from("team_members").select("teams(slug, name)")) now returns a typed result from the Database generic.
2. If the generated type types teams as an object (not array), simplify the unwrap: replace const t = Array.isArray(m.teams) ? m.teams[0] : m.teams with const t = m.teams (per research Pitfall 4 — adjust unwrap to match the generated type, do not fight it). If it types as array, keep m.teams[0].
3. The teams.map() return type { slug: string; name: string } should match the DashboardShell props — confirm the prop type and adjust if needed.

For dashboard/page.tsx:
1. Remove the eslint-disable comment at line ~44 and the as any[] cast at line ~45. The memberships query (.from("team_members").select("team_id, teams(slug, name)")) is now typed.
2. Remove the eslint-disable comment at line ~62 and the as any[] cast at line ~63. The teamShares query is now typed via the Database generic.
3. The teamShareMap at line ~60 is typed as Record<string, unknown[]> — update the value type to match the actual query result shape from the generic (the shares join row type). If the inferred type is complex, define a TeamShareRow interface in types/team.ts and use Record<string, TeamShareRow[]>.
4. Adjust the Array.isArray unwrap logic in both locations to match the generated join type shape (object vs array per research Pitfall 4).

Do NOT add new eslint-disable comments. Do NOT change the query logic (tables, columns, filters). Only fix the typing. If a generated type conflicts with runtime behavior (e.g., a nullable field), use optional chaining or nullish coalescing — do not cast to any.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -rn "eslint-disable.*no-explicit-any\|as any\[\]" "app/(dashboard)/dashboard/layout.tsx" "app/(dashboard)/dashboard/page.tsx" | wc -l | tr -d ' ' | grep -q "^0$" && echo "NO_ANY_CASTS" || echo "ANY_CASTS_FOUND"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint "app/(dashboard)/dashboard/layout.tsx" "app/(dashboard)/dashboard/page.tsx" 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx tsc --noEmit 2>&1 | grep -c "dashboard/layout\|dashboard/page" | grep -q "^0$" && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
  </verify>
  <done>
    - Zero eslint-disable no-explicit-any comments in dashboard/layout.tsx and dashboard/page.tsx
    - Zero as any[] casts in both files
    - npx eslint passes clean on both files
    - npx tsc --noEmit reports zero errors for both files
  </done>
  <acceptance_criteria>
    - Source assertion: grep -rn "eslint-disable.*no-explicit-any\|as any\[\]" in dashboard/layout.tsx and dashboard/page.tsx returns 0 matches
    - Lint assertion: npx eslint on both files exits 0
    - Build assertion: npx tsc --noEmit does not report errors in either file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Type dashboard/teams/page.tsx and dashboard/teams/[slug]/page.tsx queries</name>
  <files>app/(dashboard)/dashboard/teams/page.tsx, app/(dashboard)/dashboard/teams/[slug]/page.tsx</files>
  <read_first>
    - app/(dashboard)/dashboard/teams/page.tsx (FULL — 55 lines visible; eslint-disable at line 40, as any[] at line 41; query at lines 35-38 selects "role, teams(id, name, slug, created_by, plan, created_at)" from team_members; TeamWithRole interface at lines 10-18)
    - app/(dashboard)/dashboard/teams/[slug]/page.tsx (FULL — 159 lines; eslint-disable at lines 57, 80, 147; as any[] at line 58; as any at line 148; query at lines 50-54 selects shares join from team_shares; query at lines 67-70 count; query at lines 73-78 events)
    - types/database.generated.ts (the generated Database type — check team_members, team_shares, team_events, teams table shapes and join inference)
    - types/team.ts (TeamRole type used in teams/page.tsx)
    - types/team-event.ts (TeamEvent interface used in teams/[slug]/page.tsx line 14 for the activity feed cast at line 133)
  </read_first>
  <action>
Remove all eslint-disable no-explicit-any comments and as any[]/as any casts from both files. The Database generic from Plan 03 types these queries.

For teams/page.tsx:
1. Remove eslint-disable at line ~40 and as any[] at line ~41. The memberships query is now typed.
2. The map() converts query rows to TeamWithRole[] — if the generated type makes m.role a string literal union, the cast m.role as TeamRole at line ~50 may become unnecessary (remove it) or may need to stay if the DB types role as text. Adjust to match the generated type.
3. Adjust the Array.isArray unwrap at line ~42 to match the generated join shape.

For teams/[slug]/page.tsx:
1. Remove eslint-disable at line ~57 and as any[] at line ~58. The teamShareRows query is typed.
2. Remove eslint-disable at line ~80 and the r: any type annotation at line ~81. The validShares.reduce() callback should use the typed row shape.
3. Remove eslint-disable at line ~147 and the row: any type annotation at line ~148. The validShares.map() callback should use the typed row shape.
4. The events cast at line ~133 (events as TeamEvent[]) — if the generated type matches TeamEvent, remove the cast; if it differs, keep a cast through the correct intermediate type (not any).
5. Adjust all Array.isArray unwrap patterns to match the generated join shapes.

Define helper row interfaces in types/team.ts if the inline query shapes are complex (e.g., TeamMembershipWithTeam, TeamShareWithShare). These are consumer-side view types, not schema types.

Do NOT add new eslint-disable comments. Do NOT change query logic.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -rn "eslint-disable.*no-explicit-any\|as any\[\]\|: any" "app/(dashboard)/dashboard/teams/page.tsx" "app/(dashboard)/dashboard/teams/[slug]/page.tsx" | wc -l | tr -d ' ' | grep -q "^0$" && echo "NO_ANY_CASTS" || echo "ANY_CASTS_FOUND"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint "app/(dashboard)/dashboard/teams/page.tsx" "app/(dashboard)/dashboard/teams/[slug]/page.tsx" 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx tsc --noEmit 2>&1 | grep -c "teams/page\|teams/\[slug\]" | grep -q "^0$" && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
  </verify>
  <done>
    - Zero eslint-disable no-explicit-any comments in teams/page.tsx and teams/[slug]/page.tsx
    - Zero as any[] or : any type annotations in both files
    - npx eslint passes clean on both files
    - npx tsc --noEmit reports zero errors for both files
  </done>
  <acceptance_criteria>
    - Source assertion: grep -rn "eslint-disable.*no-explicit-any\|as any\[\]\|: any" in teams/page.tsx and teams/[slug]/page.tsx returns 0 matches
    - Lint assertion: npx eslint on both files exits 0
    - Build assertion: npx tsc --noEmit does not report errors in either file
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Flip build config flags and verify full build passes</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (FULL — 14 lines; typescript.ignoreBuildErrors at line 7, eslint.ignoreDuringBuilds at line 11)
    - .planning/phases/01-build-hygiene/01-RESEARCH.md (Code Examples section — the final next.config.ts shape per HYG-01+HYG-02)
  </read_first>
  <what-built>
All type debt and lint debt from Plans 01-03 has been cleared. The 5 @ts-nocheck files are typed, the 7 dashboard any-casts are typed, the tunnel URL is removed, and all catch blocks are narrowed. The build config flags can now be safely removed.

Before flipping the flags, the executor must confirm that NEXT_PUBLIC_API_URL is set in the Vercel Production environment — if it is not, the fail-fast throw from Plan 03 Task 2 will crash the production build on the next deploy.
  </what-built>
  <how-to-verify>
1. Run the full pre-flight check locally BEFORE editing next.config.ts:
   npm run typecheck  (must exit 0)
   npm run lint       (must exit 0)

2. If both pass, edit next.config.ts: remove the entire typescript block (lines 4-8) and the entire eslint block (lines 9-12). The file should contain only:
   import type { NextConfig } from "next";
   const nextConfig: NextConfig = {};
   export default nextConfig;

3. Run the authoritative gate:
   npm run build
   This must exit 0 with zero TypeScript errors and zero ESLint violations.

4. Run all 4 grep-gates to confirm the phase success criteria:
   grep -rl "@ts-nocheck" app/ components/ lib/ hooks/ utils/ types/  (must return empty)
   grep -rl "trycloudflare.com" lib/  (must return empty)
   grep -rn "eslint-disable.*no-explicit-any" "app/(dashboard)"  (must return empty)
   grep -rn "catch (err: any)\|catch (error: any)" app/ components/ lib/  (must return empty)

5. DEPLOYMENT PREREQUISITE CHECK (manual, before merging to main):
   Confirm NEXT_PUBLIC_API_URL is set in Vercel Project Settings → Environment Variables → Production.
   If it is NOT set, the next production deploy will fail at build time with the custom error message from api-client.ts.
  </how-to-verify>
  <resume-signal>Type "approved" if npm run build passes clean and all grep-gates return empty, or describe the failing errors</resume-signal>
  <done>
    - next.config.ts has no ignoreBuildErrors or ignoreDuringBuilds keys
    - npm run build exits 0
    - All 4 grep-gates return empty
    - NEXT_PUBLIC_API_URL confirmed set in Vercel Production (or deploy-blocking issue documented)
  </done>
  <acceptance_criteria>
    - Source assertion: grep -c "ignoreBuildErrors\|ignoreDuringBuilds" next.config.ts equals 0
    - Build assertion: npm run build exits 0
    - Grep-gate assertion: grep -rl "@ts-nocheck" app/ components/ lib/ hooks/ utils/ types/ returns empty
    - Grep-gate assertion: grep -rl "trycloudflare.com" lib/ returns empty
    - Grep-gate assertion: grep -rn "eslint-disable.*no-explicit-any" "app/(dashboard)" returns empty
    - Grep-gate assertion: grep -rn "catch (err: any)\|catch (error: any)" app/ components/ lib/ returns empty
  </acceptance_criteria>
  <reversibility rating="costly" rationale="Re-enabling build enforcement is the phase's terminal gate. Once the flags are removed and the build passes, the codebase is type-safe by construction. Re-adding the flags would be a regression that defeats the purpose of the entire phase. This task is itself a blocking human-verify checkpoint, which serves as the decision gate." />
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → production | npm run build is now the enforcement gate — any TS error or ESLint violation blocks deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | next.config.ts flag removal | medium | mitigate | Removing the flags means any future type/lint error blocks the build. This is the intended behavior — the checkpoint:human-verify task gates the flag flip on a clean full build. |
| T-04-02 | Repudiation | dashboard query typing | low | accept | Typed queries do not change runtime data access (RLS enforces that); they prevent silent shape-mismatch failures at compile time |
</threat_model>

<verification>
Phase-complete verification (all must pass):
- npm run build exits 0 (authoritative — runs tsc + eslint + bundle)
- npm run lint exits 0
- npm run typecheck exits 0
- grep -rl "@ts-nocheck" app/ components/ lib/ hooks/ utils/ types/ returns empty
- grep -rl "trycloudflare.com" lib/ returns empty
- grep -rn "eslint-disable.*no-explicit-any" "app/(dashboard)" returns empty
- grep -rn "catch (err: any)\|catch (error: any)" app/ components/ lib/ returns empty
- next.config.ts contains no ignoreBuildErrors or ignoreDuringBuilds
</verification>

<success_criteria>
- npm run build is green with type checking and linting enforced
- Zero @ts-nocheck, zero tunnel URLs, zero dashboard any-casts, zero catch(err:any)
- All 5 phase requirements (HYG-01 through HYG-05) satisfied
- NEXT_PUBLIC_API_URL deployment prerequisite documented
</success_criteria>

<output>
Create `.planning/phases/01-build-hygiene/01-04-SUMMARY.md` when done
</output>
