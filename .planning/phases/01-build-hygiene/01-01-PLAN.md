---
phase: 01-build-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/errors.ts
  - lib/team-rpc.ts
  - lib/token-security.ts
  - package.json
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npx tsc --noEmit reports zero errors for lib/team-rpc.ts, lib/errors.ts, and lib/token-security.ts"
    - "npx eslint lib/team-rpc.ts lib/errors.ts lib/token-security.ts reports zero violations"
    - "grep -c '@ts-nocheck' lib/team-rpc.ts returns 0"
    - "grep -c 'catch (error: any)\|catch (err: any)' lib/token-security.ts returns 0"
    - "No Promise<any> return type signatures remain in lib/team-rpc.ts"
  artifacts:
    - "lib/errors.ts exists and exports getErrorMessage(e: unknown): string"
    - "lib/team-rpc.ts has no @ts-nocheck directive"
    - "lib/token-security.ts has no catch (error: any) blocks"
    - "package.json has a typecheck script"
  key_links:
    - "lib/team-rpc.ts imports getErrorMessage from @/lib/errors"
    - "lib/token-security.ts imports getErrorMessage from @/lib/errors"
    - "lib/team-rpc.ts return types are typed interfaces, not Promise<any>"
---

<objective>
Establish the catch-narrowing foundation and remove @ts-nocheck from the shared dependency (lib/team-rpc.ts).

This is the TRACER task for the phase: it proves the @ts-nocheck removal architecture end-to-end on the file with the most downstream consumers. The errors helper, the catch-narrowing pattern, and the RPC return-type interfaces established here are reused by every subsequent plan.

Purpose: team-rpc.ts is imported by all 4 component files that also carry @ts-nocheck. It must be fully typed first. The getErrorMessage helper resolves all 37+ strict-mode useUnknownInCatchVariables errors across the phase DRY-ly. token-security.ts carries 2 catch (error: any) blocks that trip ESLint no-explicit-any (HYG-02).

Output: lib/errors.ts (new), lib/team-rpc.ts (typed), lib/token-security.ts (catches narrowed), package.json (typecheck script).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-hygiene/01-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create errors helper + remove @ts-nocheck from team-rpc.ts</name>
  <files>lib/errors.ts, lib/team-rpc.ts, package.json</files>
  <read_first>
    - lib/team-rpc.ts (FULL — 544 lines; the @ts-nocheck is line 1, identify all catch blocks with error.message access, all Promise<any> returns at lines 287/306/321, and the RPC method signatures)
    - types/team.ts (existing interfaces: TeamRole, TeamInvite, RateLimitInfo at lines 53-72)
    - utils/supabase/server.ts (the createAdminClient function imported by team-rpc.ts line 10)
    - next.config.ts (current ignoreBuildErrors config — confirms these files are NOT being type-checked today)
    - tsconfig.json (confirms strict: true at line 7, which implies useUnknownInCatchVariables)
  </read_first>
  <action>
Step 1 — Create lib/errors.ts. Export a single function getErrorMessage(e: unknown): string that returns e.message if e instanceof Error, otherwise String(e). This is the DRY helper used by every catch block across the phase (per HYG-03 catch-narrowing pattern from research Pattern 3). No other exports. No comments.

Step 2 — Add typecheck script to package.json scripts: "typecheck": "tsc --noEmit". This enables fast TS-only feedback for every subsequent task. Also add "check:hygiene": "tsc --noEmit && next lint" for the full gate. Insert after the existing "lint" script.

Step 3 — Remove the // @ts-nocheck directive from line 1 of lib/team-rpc.ts. Fix every surfaced error:

3a. Narrow all catch blocks: Every catch (error) { ... error.message ... } becomes catch (error) { const msg = getErrorMessage(error); ... msg ... }. Import getErrorMessage from @/lib/errors at the top. The error.message accesses appear in two patterns: throw new Error(`... ${error.message}`) at lines ~108/130/147/165/184/202/221/240/257/278/297/312/327, and if (error.message.includes('...')) at lines ~366/411/414/417/444/447/450. Replace error.message with getErrorMessage(error) in all cases. For the throw-pattern, the message is interpolated: throw new Error(`Failed to create team: ${getErrorMessage(error)}`). For the includes-pattern, assign const msg = getErrorMessage(error) then test msg.includes('...').

3b. Type the 3 Promise<any> return signatures at lines 287, 306, 321. Define interfaces for each RPC return shape:
  - GlobalRateLimitResult for checkGlobalRateLimit (line 287): fields is_limited (boolean), hourly/daily counts. Tag field names as [ASSUMED] in the interface — verify exact shape by checking the RPC function definition. Return data as GlobalRateLimitResult.
  - RateLimitingStats for getRateLimitingStats (line 306): a stats object shape. Return data as RateLimitingStats.
  - CleanupResult for cleanupRateLimitingData (line 321): a cleanup summary. Return data as CleanupResult.
  IMPORTANT: The exact field names of these RPC returns are [ASSUMED] (per research Assumption A1). Define reasonable interfaces based on the RPC function names and parameter context. Use the as cast at the RPC boundary (data as InterfaceName) — this is the one legitimate cast site for RPC results per research Pitfall 3. If the generated types (Plan 03) later show a different shape, these interfaces can be adjusted.

3c. Fix any remaining TS errors surfaced by removing @ts-nocheck: check for untyped variables, implicit any parameters, and missing return types on exported functions. The TeamRPC class methods that return data without a type annotation (createTeam at line 99, transferOwnership at line 156, updateMemberRole at line 174, removeMember at line 192, addMember at line 210, bulkInvite at line 226, revokeInvite at line 237, checkRateLimit at line 258) should have explicit return types or rely on inference from the RPC data. Add return type annotations where the inferred type is too loose.

Do NOT touch any file other than lib/errors.ts, lib/team-rpc.ts, and package.json. Do NOT remove @ts-nocheck from the 4 component files — that is Plan 02. Do NOT modify next.config.ts — that is Plan 04.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx tsc --noEmit 2>&1 | grep -c "team-rpc\|errors\.ts" | grep -q "^0$" && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint lib/team-rpc.ts lib/errors.ts 2>&1 | grep -c "error" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -v '^//' lib/team-rpc.ts | grep -c "@ts-nocheck" | grep -q "^0$" && echo "NOCHECK_REMOVED" || echo "NOCHECK_PRESENT"</automated>
  </verify>
  <done>
    - lib/errors.ts exports getErrorMessage(e: unknown): string
    - lib/team-rpc.ts has zero @ts-nocheck directives
    - lib/team-rpc.ts has zero Promise<any> return types
    - npx tsc --noEmit reports zero errors referencing team-rpc.ts or errors.ts
    - npx eslint on lib/team-rpc.ts and lib/errors.ts reports zero violations
    - package.json scripts include typecheck and check:hygiene
  </done>
  <reversibility rating="costly" rationale="Creates a new shared utility (lib/errors.ts) that subsequent plans depend on; removing team-rpc.ts @ts-nocheck is the one-way gate that makes all 4 component files fixable. Reverting would require re-adding @ts-nocheck." />
</task>

<task type="auto">
  <name>Task 2: Narrow catch blocks in token-security.ts</name>
  <files>lib/token-security.ts</files>
  <read_first>
    - lib/token-security.ts (FULL — identify the 2 catch (error: any) blocks at lines 270 and 325, understand the error.message access patterns and the return shapes of the enclosing functions)
    - lib/errors.ts (the getErrorMessage helper created in Task 1 — confirm its signature before importing)
  </read_first>
  <action>
Fix the 2 catch (error: any) blocks in lib/token-security.ts that trip ESLint @typescript-eslint/no-explicit-any (per HYG-02). Both blocks are in methods that catch and return structured error objects:

At line ~270: catch (error: any) returns { valid: false, error: error.message || 'Failed to validate invite', actionRequired: '...' }. Change to catch (error) and replace error.message with getErrorMessage(error). Import getErrorMessage from @/lib/errors at the top of the file.

At line ~325: catch (error: any) returns { success: false, error: error.message, message: 'Failed to accept invite' }. Same change: catch (error) and getErrorMessage(error).

These are the only catch (error: any) blocks in this file. Do NOT change empty catch blocks (those are Phase 3 scope per SEC-05). Do NOT change any other logic in the file.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && grep -c "catch (error: any)\|catch (err: any)" lib/token-security.ts | grep -q "^0$" && echo "NO_ANY_CATCH" || echo "ANY_CATCH_FOUND"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx eslint lib/token-security.ts 2>&1 | grep -c "no-explicit-any" | grep -q "^0$" && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
    <automated>cd /Users/ddphuong/Projects/next-labs/dropitx/dropitx && npx tsc --noEmit 2>&1 | grep -c "token-security" | grep -q "^0$" && echo "TSC_PASS" || echo "TSC_FAIL"</automated>
  </verify>
  <done>
    - Zero catch (error: any) or catch (err: any) blocks in lib/token-security.ts
    - npx eslint lib/token-security.ts reports zero no-explicit-any violations
    - npx tsc --noEmit reports zero errors referencing token-security.ts
  </done>
  <acceptance_criteria>
    - Source assertion: grep -c "catch (error: any)\|catch (err: any)" lib/token-security.ts equals 0
    - Source assertion: grep -c "getErrorMessage" lib/token-security.ts equals at least 2 (one per catch block)
    - Lint assertion: npx eslint lib/token-security.ts exits 0
    - Build assertion: npx tsc --noEmit does not report errors in token-security.ts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module import boundary | lib/errors.ts is a new shared module consumed by catch blocks across the codebase — its behavior must be deterministic for all input types |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | lib/errors.ts getErrorMessage | low | mitigate | The helper handles unknown input via instanceof check + String() fallback; no parsing or eval; safe for all error shapes |
| T-01-02 | Information Disclosure | lib/team-rpc.ts catch blocks | low | accept | Error messages from Supabase RPC are surfaced to users via throw; this is existing behavior unchanged by the narrowing refactor |
</threat_model>

<verification>
After both tasks:
- npx tsc --noEmit passes clean for lib/team-rpc.ts, lib/errors.ts, lib/token-security.ts (other @ts-nocheck files still skipped by TS)
- npx eslint on all three files passes clean
- grep -rl "@ts-nocheck" lib/ returns empty (lib is fully typed)
- grep -rl "catch (err: any)\|catch (error: any)" lib/ returns empty
- npm run typecheck is runnable (added to package.json)
</verification>

<success_criteria>
- lib/errors.ts exists with getErrorMessage(e: unknown): string
- lib/team-rpc.ts compiles under strict mode with no @ts-nocheck
- lib/token-security.ts has no catch (error: any) blocks
- package.json has typecheck and check:hygiene scripts
- npx tsc --noEmit and npx eslint pass on all three modified lib files
</success_criteria>

<output>
Create `.planning/phases/01-build-hygiene/01-01-SUMMARY.md` when done
</output>
